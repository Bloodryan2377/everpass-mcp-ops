{
  "_comment": "SessionStart banner for productivity stack. MERGE the SessionStart array into settings.json hooks — never replace the whole file. Script path assumes ops repo at C:\\Users\\ryan\\code\\everpass-mcp-ops. Adjust if your clone path differs. Banner is read-only (prints only).",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "powershell -NoProfile -ExecutionPolicy Bypass -File \"C:\\Users\\ryan\\code\\everpass-mcp-ops\\scripts\\stack-session-banner.ps1\""
          }
        ]
      }
    ]
  }
}
